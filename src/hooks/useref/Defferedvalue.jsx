export const DegfferedValue=()=>{
    return(
        <>
        {/* usedeffedvalue
        

        useref: (to focus for particalr field)
        usseref stores the mutable value, it do not causes and rendering when the value get update
        it allows you to access directly the dom element => uncontrolled components

        classcompoent
        -createref to avoid the re-rendering
         2018
         

        */}
        </>
    )
}